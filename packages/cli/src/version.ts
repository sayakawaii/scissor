/**
 * The CLI's version string. Kept in one place so `--version` and the benchmark
 * artifact's provenance can never disagree about which scissor produced a run.
 */
export const VERSION = "0.2.0";
